<h1>The Jobs EJS Application</h1>

<% if (errors.length > 0) { %>
  <% errors.forEach(err => { %>
    <div style="color:red;"><%= err %></div>
  <% }) %>
<% } %>

<% if (info.length > 0) { %>
  <% info.forEach(msg => { %>
    <div style="color:green;"><%= msg %></div>
  <% }) %>
<% } %>

<hr>
